import { createMuiTheme } from 'material-ui/styles';
import { darkBlack, grey600 } from 'material-ui/colors/grey';

const roverViewTheme = createMuiTheme({
  palette: {
    primary: darkBlack,
    secondary: grey600,
  },
});